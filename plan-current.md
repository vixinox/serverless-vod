# 开发者计划（第二阶段）

> 更新时间：2026-03-19
>
> 当前主线：在现有 `lib/server/* + app/api/* + apiRequest` 架构上，补齐演示链路里还缺的页面和交互，不再继续扩展旧的 Server Action 方案。

---

## 当前真实状态

### 已完成的主链路
- 首页视频流、短视频流、播放页过渡动画
- HLS 播放器、双击快进/暂停反馈
- 点赞、订阅、收藏、分享
- 评论 / 回复 CRUD、排序、点赞
- 搜索页、历史记录页、设置页
- Studio 视频管理、播放列表管理、上传、处理状态、重试、取消
- Studio Dashboard 真数据
- Studio Stat 真数据表格页

### 当前架构事实
- 客户端交互主路径已经统一为：`client -> app/api/* -> lib/server/*`
- `components/`、`app/`、`hooks/` 中已没有客户端直接调用 `@/actions/*`
- 旧 `actions/*` 只保留少量兼容封装；不再作为后续功能主入口

### 已关闭的阶段性工作
- Server Action 收尾迁移已完成
- 独立的 server-action 收尾文档已移除，不再单独维护

---

## 还没做完的核心缺口

### P0：最值得马上做

#### 1. 频道主页 `/channel/[name]`
- 现状：
  - 频道订阅能力已经可用
  - 播放页有频道信息区，但没有频道详情页
  - 仓库里也还没有 `app/channel/[name]/page.tsx`
- 为什么优先：
  - 这是当前演示动线里最明显的断点
  - “点进频道继续看内容”是视频站最自然的链路
- 建议结果：
  - 频道头图 / 头像 / 名称 / 订阅数 / 订阅按钮
  - 频道视频列表
  - 至少支持从播放页频道信息点击进入

#### 2. 播放页频道跳转接通
- 现状：
  - `components/player/channel-info.tsx` 已支持订阅，但频道名仍是纯文本
- 为什么优先：
  - 依赖小，完成后整条观看链路会更完整
- 建议结果：
  - 频道名、头像区域变成可点击入口
  - 跳转到 `/channel/[name]`

---

## 第二优先级

### P1：答辩展示加分项

#### 3. `studio/stat` 图表化
- 现状：
  - `app/studio/stat/page.tsx` 已接通真实数据
  - 当前仍然是表格展示，页面里也明确写了“后续可接 ECharts”
- 建议结果：
  - 折线图：近 30 天观看趋势
  - 柱状图：视频播放量对比
  - 保留现有表格作为降级展示或附加明细

#### 4. “添加到”升级为可选播放列表弹窗
- 现状：
  - 播放页现在的“添加到”本质上是快捷收藏到系统列表
  - Studio 已有完整播放列表 CRUD 能力
- 建议结果：
  - 弹窗选择任意播放列表
  - 支持边建列表边加入
  - 保留“快速收藏”作为默认捷径

---

## 第三优先级

### P2：体验补强

#### 5. 评论内时间戳点击跳转
- 现状：
  - 评论内容目前还是普通文本
  - 播放器本身已有 seek 能力
- 建议结果：
  - 识别 `MM:SS` / `HH:MM:SS`
  - 点击后让播放器跳转到对应时间点

#### 6. 观看行为埋点细化
- 现状：
  - 当前已写入 `PLAY_START / ENDED`
- 建议结果：
  - 增加 `PAUSE / SEEK / PROGRESS`
  - 为后续统计页和答辩说明提供更细粒度数据

---

## 推荐的下一步执行顺序

### Sprint A：补齐频道链路
1. 在 `lib/server/channels.ts` 增加频道详情读取能力
2. 新建 `app/channel/[name]/page.tsx`
3. 把 `components/player/channel-info.tsx` 接成可跳转入口
4. 跑 `bunx tsc --noEmit`

### Sprint B：提升展示效果
1. 给 `studio/stat` 接入图表
2. 保留当前表格作为数据明细
3. 如果统计表为空，补 seed / rollup 演示数据

### Sprint C：补交互体验
1. 评论时间戳跳转
2. “添加到播放列表”弹窗化
3. 视时间决定是否补更细粒度播放事件

---

## 当前建议

如果下一步只做一件事，就做“频道主页 + 播放页跳转频道”。  
这是当前最缺、最容易被注意到、同时对演示完整度提升最大的一项。

---

## 验证方式

- 功能改动优先跑：`bunx tsc --noEmit`
- 路由改动优先补：`bun test`
- `bun run lint` 目前仍不适合作为稳定校验依据
